#HTTP- Hypertext Transfer protocol
It's a system of rule that serve as a link between apllications and the transfer of **hypertext** document.
HTTP follow a simple model where a model make a **request** to a server and wait for a response. Also refer as **request reaponse protocol**.
##IP address - Internet Protocol Address
An IP address acts as the identifier for a device or serverm and can contain hundredsor thousand of portsm each used for a different communication purpose to that device ro server. IP address provide by **Internet Service Provider**. when we connect to Google's main page we do not type in the IP Addressm we type in **URL**.
##DNS
This mapping is of **URL** to **IP address** is handling by the Domain Naming System or **DNS**.

![](http://d186loudes4jlv.cloudfront.net/http/images/internet.png)

##Resources
Resource is a gerneric tern for things you interact with on the internet via a **URL**. THis include images, videos, web pages.

![](http://d186loudes4jlv.cloudfront.net/http/images/resources.png)

#Stateless
A protocol is said to be **stateless** when it's designed in such a way that each request/response pair is completely independent of the previous
 one. **HTTP** is a **stateless** protocol, but there are tircks web developers and frameworks employ to make it seeem like the application is stateful.