- **session id is stored on the client, and it is used as a "key" to the session data stored server side.**
- **To recap, we've seen that the session data is generated and stored on the server-side and the session id is sent to the client in the form of a cookie.**

#AJAX - Asynchronous Javascript abd XML
It's allows browsers to issue request and process responses without a full page refresh.